name: EXOVANT Vanta Rev10 Material Traversal Probe
on:
  pull_request:
concurrency:
  group: vanta-rev10-material-traversal-${{ github.ref }}
  cancel-in-progress: true
jobs:
  probe:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Sandbox qualification superseded this queued canary."
