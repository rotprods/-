name: EXOVANT Vanta Maintenance Hold
on:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  hold:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Vanta maintenance hold"
