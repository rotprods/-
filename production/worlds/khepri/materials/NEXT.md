# NEXT — KHEPRI Materials X100

## Immediate critical path

1. `MAIN_REGISTRY_ACTIVE_ACK`
   - serialize `CLM-KHEPRI-MATERIALS-001` into published main registry;
   - owner ACK epoch 1;
   - readback status `active`;
   - no provider/project ID collision.

2. `PRODUCTION_TEXTURE_TIER_QUALIFICATION`
   - input: target hardware + streaming/virtual-texture policy;
   - decide final source/runtime resolution tiers by scale and material family;
   - decide mip policy, compression target, maximum slots and memory budget;
   - calibration 128² MUST NOT be promoted as production texture art.

3. `ONE_CONSUMER_PILOT`
   - preferred consumer: `KHP_WM_HELIOSTAT_FOOTPRINTS` support geometry;
   - requires explicit geometry-owner handoff;
   - apply optical mirror + Sínodo bronze family without modifying geometry ownership;
   - export/import Godot and record visual/runtime before/after receipt.

## Subsequent X100 expansion

- production-resolution material sources per family;
- application-specific trim/tile/decal strategy where justified;
- close-range hero inserts only where player distance needs them;
- state transitions driven by story/gameplay tags, not random assignment;
- apply to Ciudad de los Toldos / Mar de Cristal / Crisol de RAKHET only through their future domain claims.

## Stop conditions

Stop or isolate when a step requires:
- final Glass Sea physical-medium canon;
- atmospheric chemistry;
- target hardware budget not yet qualified;
- cross-scope geometry mutation without handoff;
- human final-art approval.
